<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fastcampus.web3.dao.TmpUserMapper">
  <select id="select" parameterType="String" resultType="TmpUser">
    SELECT *
    FROM user_test
    WHERE id = #{id}
  </select>
</mapper>